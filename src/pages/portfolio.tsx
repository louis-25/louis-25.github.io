import React, { FunctionComponent } from 'react'

type portfolioProps = {}

const portfolio: FunctionComponent<portfolioProps> = ({ children }) => {
  return <h1>포트폴리오 페이지</h1>
}

export default portfolio
